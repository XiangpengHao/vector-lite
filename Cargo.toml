[package]
name = "vector-search"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.9.0"

[dev-dependencies]
criterion = "0.5.1"

[[bench]]
name = "ann_benchmark"
harness = false


[profile.bench]
inherit = ["release"]
lto = true
